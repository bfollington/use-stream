{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.18.4"
  },
  "scripts": {
    "netlify": "yarn bootstrap && lerna exec -- yarn build",
    "publish": "lerna publish",
    "changed": "lerna changed",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean"
  }
}
